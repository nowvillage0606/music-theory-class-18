---
// src/components/MiniBar.astro
interface Props {
  val: number;
  max?: number;
}
const { val, max = 5 } = Astro.props;
---

<div class="flex gap-1 mt-1">
  {Array.from({ length: max }).map((_, i) => (
    <div class={`h-1 w-6 rounded-full ${i < val ? "bg-white" : "bg-white/10"}`} />
  ))}
</div>
