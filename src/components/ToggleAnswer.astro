---
const { title = "開く", open = false } = Astro.props;
---
<details class="group rounded-lg border border-slate-200 bg-slate-50 p-4 text-slate-900" {...(open ? { open: true } : {})}>
  <summary class="cursor-pointer select-none font-semibold flex items-center gap-2 text-slate-800">
    <span class="ms">expand_more</span><span>{title}</span>
  </summary>
  <div class="mt-3 text-sm leading-relaxed">
    <slot />
  </div>
</details>