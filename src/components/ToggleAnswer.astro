---
const { title = "答えを見る", open = false } = Astro.props;
---
<details class="group rounded-lg border border-white/15 bg-white/5 p-4" {...(open ? { open: true } : {})}>
  <summary class="cursor-pointer select-none font-semibold flex items-center gap-2">
    <span class="ms">expand_more</span><span>{title}</span>
  </summary>
  <div class="mt-3 text-sm leading-relaxed">
    <slot />
  </div>
</details>