---
const { title, good, pitfall, fix, icon = "handyman" } = Astro.props;
---
<div class="rounded-xl border border-slate-200 bg-slate-50 p-5 text-slate-900">
  <div class="flex items-center gap-2 text-sm text-slate-600">
    <span class="ms">{icon}</span><span>パターン</span>
  </div>
  <h3 class="mt-2 text-lg font-semibold text-slate-900">{title}</h3>
  <ul class="mt-3 space-y-2 text-sm leading-relaxed">
    {good && <li><strong class="text-emerald-700">本質：</strong>{good}</li>}
    {pitfall && <li><strong class="text-amber-700">罠：</strong>{pitfall}</li>}
    {fix && <li><strong class="text-sky-700">対処：</strong>{fix}</li>}
  </ul>
</div>