---
const { title, good, pitfall, fix, icon = "handyman" } = Astro.props;
---
<div class="rounded-xl border border-white/15 bg-white/5 p-5">
  <div class="flex items-center gap-2 text-sm text-slate-300">
    <span class="ms">{icon}</span><span>パターン</span>
  </div>
  <h3 class="mt-2 text-lg font-semibold">{title}</h3>
  <ul class="mt-3 space-y-2 text-sm leading-relaxed">
    {good && <li><strong class="text-emerald-300">本質：</strong>{good}</li>}
    {pitfall && <li><strong class="text-amber-300">罠：</strong>{pitfall}</li>}
    {fix && <li><strong class="text-sky-300">対処：</strong>{fix}</li>}
  </ul>
</div>